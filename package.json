{
  "name": "sam-teaching-visual",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Browser teaching studio with whiteboard, recording, Google Drive upload and FFmpeg post-production.",
  "scripts": {
    "lint": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run lint && npm test && npm run build",
    "serve": "node scripts/serve.mjs"
  },
  "engines": {"node": ">=20"}
}
